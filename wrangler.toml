# Minimal wrangler.toml — environment-agnostic (no env blocks)
# Account-specific values (account_id, zone_id) are provided at deploy time via CI/secrets

name = "my-worker"
main = "src/index.js"
compatibility_date = "2026-08-13"
workers_dev = true

# Keep environment-specific config out of the committed file.